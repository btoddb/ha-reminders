{
  "config": {
    "step": {
      "user": {
        "title": "BToddB Reminders",
        "description": "BToddB Reminders are delivered as a notification through a notify service.",
        "data": {
          "calendar_name": "Calendar name",
          "notify_service": "Notify service"
        },
        "data_description": {
          "calendar_name": "Name for the calendar entity (e.g. \"BToddB Reminders\"). Determines the entity_id.",
          "notify_service": "Pick the notify service to push due reminders to (e.g. notify.persistent_notification, notify.mobile_app_DEVICE, or a notify group). See the README Configure section."
        }
      }
    },
    "abort": {
      "single_instance_allowed": "BToddB Reminders is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "BToddB Reminders",
        "data": {
          "notify_service": "Notify service"
        },
        "data_description": {
          "notify_service": "Pick the notify service to push due reminders to (e.g. notify.persistent_notification, notify.mobile_app_DEVICE, or a notify group). See the README Configure section."
        }
      }
    }
  },
  "services": {
    "create": {
      "name": "Create reminder",
      "description": "Add a time-based reminder at an absolute time or a relative delay.",
      "fields": {
        "message": {
          "name": "Message",
          "description": "What to remind about, in the user's own words."
        },
        "when": {
          "name": "When",
          "description": "Absolute ISO 8601 local datetime for a specific clock time or date. Leave empty for relative delays and use in_minutes instead."
        },
        "in_minutes": {
          "name": "In minutes",
          "description": "Minutes from now, for relative delays. Takes priority over when when both are given."
        }
      }
    },
    "create_location": {
      "name": "Create location reminder",
      "description": "Add a reminder delivered the moment a person enters or leaves a zone. Fires once, then stays visible for 7 days.",
      "fields": {
        "message": {
          "name": "Message",
          "description": "What to remind about, in the user's own words."
        },
        "person": {
          "name": "Person",
          "description": "The person entity whose location triggers the reminder."
        },
        "zone": {
          "name": "Zone",
          "description": "The zone to watch."
        },
        "trigger": {
          "name": "Trigger",
          "description": "Deliver when the person enters the zone, or when they leave it."
        }
      }
    },
    "delete_location": {
      "name": "Delete location reminder",
      "description": "Remove a location reminder by its unique id.",
      "fields": {
        "uid": {
          "name": "UID",
          "description": "The unique id of the location reminder to delete."
        }
      }
    }
  }
}
